<div class="container textarea">
  <div class="row">
    <div class="span6 offset3">
      <%= form_for(@post) do |f| %>
        <% if @post.errors.any? %>
          <div id="error_explanation">
            <div class="alert alert-error">
            <%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:

            <ul>
            <% @post.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

       
          <%= f.label :title, "Projektname" %><br />
          <%= f.text_field :title, placeholder: "Name ..." %>
       
       
          <%= f.label :text, "Kurzbeschreibung" %><br />
          <%= f.text_area :text, :size => '40x10', placeholder: "Füge eine Projektbeschreibung hinzu ..."%>
       
       
          <%= f.submit "abschicken", class: "btn btn-large btn-inverse" %>
      <% end %>
      </div>
    </div>
  </div>
</div>